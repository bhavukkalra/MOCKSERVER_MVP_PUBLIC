{
  "name": "nodeserver",
  "version": "1.0.0",
  "description": "Node Backend for handling incoming request from Nuxt and providing a valid response",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "cd client && npm install && npm run build && npm run generate"
  },
  "author": "bhavuk Kalra",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "firebase-admin": "^9.8.0",
    "json-server": "^0.16.3"
  }
}
